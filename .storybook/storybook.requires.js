/* do not change this file, it is auto generated by storybook. */

import {
  configure,
  addDecorator,
  addParameters,
  addArgsEnhancer,
  clearDecorators,
} from '@storybook/react-native';

import { docsTitle, extractComponentDescription, enhanceArgTypes } from '@storybook/docs-tools';
import { DecoratorFunction } from '@storybook/addons';
import { loadStories, stories } from './storyLoader';

if (!global.STORIES_CONFIGURED) {
  loadStories();
  global.STORIES_CONFIGURED = true;
}

const getStories = () => {
  return configure(() => {
    loadStories();
  }, module, false);
};

export const view = getStories();